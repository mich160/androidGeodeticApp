<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="student.pl.edu.pb.geodeticapp">
    <application
        android:allowBackup="true"
        android:icon="@drawable/geodeticapp_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/geodeticapp_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activities.MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.PointsActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activities.SettingsActivity"
            android:launchMode="singleTask" />
        <activity android:name=".activities.EditPointActivity" />
        <activity
            android:name=".activities.AboutActivity"
            android:launchMode="singleTask" />
    </application>

</manifest>